package View.ScenesAndMainGroupView;

import View.View;
import javafx.scene.Group;
import javafx.scene.Scene;
import javafx.stage.Stage;

public class ProductInHelicopterView extends View {
    private Group rootProductInHelicopterView=new Group();
    private Scene sceneProductInHelicopterView=new Scene(rootProductInHelicopterView,1600,900);

    public ProductInHelicopterView(Stage primaryStage){
        Start(primaryStage);
    }

    @Override
    public void Start(Stage primaryStage) {

    }
}
